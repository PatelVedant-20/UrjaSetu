"""Phase 6-8 verification: pricing, settlement and audit as one system.

These tests exist because Phases 6, 7 and 8 shipped with no test coverage at
all. They verify the chain the architecture describes, end to end, against a
real PostgreSQL database.
"""
